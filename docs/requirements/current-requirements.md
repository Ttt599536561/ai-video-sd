# 当前需求清单

## 用户端

- ~~文生视频和图生视频合并为一个 Tab。~~
- ~~首尾帧改为视频生视频 Tab；文/图生视频只保留参考图或首帧上传。~~
- ~~参考图和参考视频上传区域支持点击打开系统文件选择器，并在悬浮时显示可点击状态。~~
- ~~分辨率使用下拉框，支持 480P、720P、1080P。~~
- ~~视频时长限制为 5 到 15 秒。~~
- ~~新增登录注册页，仅邮箱注册登录，不需要邮箱验证码。~~
- ~~新增兑换积分入口。~~
- ~~普通用户端不显示任务队列左侧菜单，不显示“最近生成”。~~
- ~~视频生成页任务队列无任务时显示“暂无视频队列”。~~
- ~~视频生成页任务队列最多展示 10 条，超出在固定高度内滚动；队列卡片底边与“生成参数”卡片底边对齐。~~
- ~~用户端普通用户不显示管理后台入口；后台页也必须验证管理员角色后才展示操作。~~
- ~~兑换积分页默认不展示用户邮箱，兑换码输入框避免浏览器自动填入邮箱。~~
- ~~账号管理页为白底卡片样式，新密码和确认新密码输入框对齐。~~
- ~~用户端左下角显示“可用积分”，进度条随当前可用积分同步。~~
- ~~用户端模型、套餐、视频任务队列和项目页已改为后端数据驱动；生成任务、项目视频删除和 Mock 任务处理调用后端。~~
- ~~视频生成任务队列和项目视频资产已解耦：任务队列不可删除，项目页删除视频会删除数据库中的视频资产记录，但不影响任务记录。~~
- ~~用户兑换记录已补后端列表和用户端脱敏展示。~~
- ~~视频生成页右侧预览区已补空状态、生成中状态、完成后自动显示视频和 1.5 秒轮询；全局提示改为页面正中间展示。~~
- ~~点击“生成视频”并通过基础校验后，立即弹出“正在提交生成任务，请稍候...”，真实供应商提交成功后再提示已提交。~~
- ~~视频生成页已移除“最新已下载视频”、播放器说明、刷新/复制/下载按钮、写死视频标题和 3 天提示背景区域。~~
- ~~项目页播放按钮已改为项目卡片内联播放，下载按钮直接下载文件。~~
- ~~用户端新增轻量“生成记录”页，展示生成时间、模型名称、提示词、尺寸/分辨率、时长、媒体上传数量、扣除积分、状态和视频生成时长；去掉完成时间，不展示视频地址或下载链接。~~
- ~~参考图/参考视频/参考音频已进入创建任务 payload，真实供应商提交会带 `images`、`videos`、`audios`。~~
- ~~用户上传参考图片后，上传区域会显示已选择图片缩略图列表和文件名。~~
- ~~参考图片提交前会在前端压缩成长边 1280px 的 JPEG data URL；后端会拒绝超大的图片 data URL，减少供应商 `fail_to_fetch_task` 失败。~~
- ~~用户端参考素材数量限制已更新：图片最多 4 张、视频最多 3 个、音频最多 1 个；参考视频+参考音频原始文件总大小限制为 36MB；后端接口同步校验。~~
- ~~失败视频任务会在队列卡片中显示后端保存的失败原因摘要。~~
- ~~用户点击积分套餐购买按钮时，会跳转到管理员为该套餐配置的购买 URL；未配置 URL 时显示提示。~~
- ~~用户端兑换积分页和兑换记录新增“有效期”，单位为天；左下角可用积分新增有效期倒计时，显示“还剩 N 天”、永久或 `--`。~~
- ~~项目页点击播放按钮不再弹出提示，直接在卡片内联播放器播放。~~
- ~~生成参数区所有下拉框已统一为自定义 `.select-control` 样式。~~
- ~~已删除“文生视频和图生视频合并为一个入口；视频生视频独立上传参考视频。”说明文案。~~
- ~~项目页切换后默认展示真实视频首帧封面，减少黑色视频封面；无需先点击播放才看到画面。~~
- ~~已删除参考素材区域和项目页中用户要求移除的说明文案。~~

## 管理端

- ~~用户管理放到左侧独立菜单。~~
- ~~用户管理显示注册时间、现有积分、购买套餐、状态、邮箱。~~
- ~~模型启用/禁用使用下拉框。~~
- ~~套餐配置支持编辑套餐名称、价格、有效期、积分数等信息。~~
- ~~新增兑换码生成功能，支持批量生成、选择数量、复制兑换码。~~
- ~~管理端兑换码生成已接入后端和 PostgreSQL，支持永久有效兑换码。~~
- ~~管理后台页面默认不暴露操作界面，必须通过 `/api/me` 确认当前用户为管理员后才显示；普通用户直接访问会跳回用户端。~~
- ~~管理端模型、套餐、用户和视频记录列表已接后端数据；模型/套餐新增更新、套餐删除、用户封禁解封和积分调整已接真实 API。~~
- ~~模型、积分套餐和兑换码操作台默认空；保存即新增或更新，保存后清空，避免“新增/编辑”语义混淆。~~
- ~~套餐和模型删除、启用/禁用失败时显示明确错误。~~
- ~~用户管理列表不显示管理员账号。~~
- ~~模型删除支持历史记录引用场景：已引用模型软删除并隐藏配置，生成记录保留。~~
- ~~审计和更完整的全局错误提示已补齐第一版。~~
- ~~积分套餐操作台新增购买跳转 URL 配置，并随套餐新增/编辑保存。~~
- ~~管理员后台视频记录已展示提示词、模型展示名/供应商模型 ID、尺寸、分辨率、用户选择的视频时长、参考图片/视频/音频数量和视频生成时长。~~
- ~~管理员后台兑换码管理页面已改为上下布局：批量生成在上方，全部兑换码记录在下方；记录区固定高度并分页切换，不用滚动条切换长列表。~~
- ~~管理员后台兑换码记录新增“有效期”，单位为天。~~
- ~~兑换码生成有效期改为“永久有效/自定义天数”，不再要求选择指定日期。~~
- ~~管理员后台复制兑换码时复制明文完整兑换码，不复制加密/密文字段。~~
- ~~管理员后台系统提示统一改为网站正中间弹窗；切换模型配置、积分套餐、用户管理、生成记录、操作审计等页签不弹提示。~~
- ~~管理员后台新增系统设置，可填写公网 API 地址供供应商抓取参考素材。~~

## 后端

- ~~搭建 Fastify + TypeScript 后端服务。~~
- ~~实现邮箱注册登录、JWT、管理员接口、兑换码校验和积分入账基础能力。~~
- ~~设计 Prisma/PostgreSQL 数据模型。~~
- ~~接入 PostgreSQL/Prisma 持久化，解决重启后用户、积分、兑换码丢失问题。~~
- ~~本地 Mock 视频任务闭环已具备：任务可流转到成功/失败，成功写入输出资产，失败退还积分且幂等。~~
- ~~CORS 已允许 `GET,HEAD,POST,PATCH,DELETE`；前端空 body 请求不再默认发送 JSON content-type。~~
- ~~真实供应商只读模型列表已接通，管理端模型名称从供应商读取为输入建议且支持手动输入真实模型 ID，用户端展示名 `displayName` 与真实供应商模型 ID `modelName` 已分离。~~
- ~~模型配置加密主密钥已改为稳定环境变量读取。~~
- 模型 URL 和 Key 必须保存在后端，不允许进入前端。
- 用户兑换码必须和管理员后台生成的兑换码一致才允许兑换。
- 用户视频文件和管理员可见视频记录只保留 3 天。
- ~~真实视频生成供应商提交/状态查询、文件存储、定时清理、审计日志和生产级模型 Key 轮换策略均已接入第一版。~~
- ~~真实供应商参考素材提交已修复：后端把 data URL 参考图片/视频/音频保存为 reference asset，再向供应商提交公网 URL，匹配供应商 `images`、`videos`、`audios` 示例。~~
- ~~供应商抓取参考素材所需公网 API 地址可在后台系统设置中配置；新增 `system_settings` 持久化表和 `/api/admin/system-settings`。~~
- ~~公网 API 地址 HTTPS 证书过期/不可验证时，后端返回 `PUBLIC_API_BASE_URL_CERT_INVALID` 的友好错误提示。~~
- ~~部署文档已补齐：包含 GitHub 推送、Debian 12 服务器部署、端口冲突处理、公网 IP/域名、后台管理启动、积分套餐/模型配置/URL/Key 是否需要重配，以及同服务器部署多个项目的注意事项。~~
- ~~生产部署和站点图标补丁已完成：用户已确认系统上线成功，并已成功更新 `favicon.svg` 补丁。~~
- ~~积分套餐持久化新增 `purchase_url` 字段，并通过 `/api/credit-packages`、`/api/admin/credit-packages` 返回；后端校验购买 URL 仅允许 HTTP/HTTPS。~~
- ~~视频任务记录持久化新增画幅/尺寸和参考图片/视频/音频数量；生成记录接口返回 `generationDurationSeconds`，由完成时间减去任务提交时间得到，单位为秒。~~
- 当前生产部署已上线：`VIDEO_PROVIDER_REAL_JOBS=true` 时用户点击生成视频会提交供应商 `POST /v1/videos`；用户反馈真实生成已能成功生成视频。下一步观察真实生成闭环、参考素材公网抓取和证书有效性，同时继续补审计筛选分页导出、备份与回滚演练。
- 已上线系统应用普通代码补丁时，生产数据库内的管理员账号、模型配置、供应商 URL/Key、积分套餐、系统设置、兑换码和用户积分会保留；只有换数据库、清空数据库或恢复另一份备份时才需要重新初始化。
- 当前阶段进入优化和完善：优先提升用户端生成体验、管理后台操作效率、真实生成稳定性和生产运维可观测性。
