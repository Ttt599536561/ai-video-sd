# AI 生视频项目全局文档

## 背景

项目目标是搭建一个面向普通用户的 AI 生视频网站，并配套一个管理员后台。用户端要覆盖从注册登录、选择生成模式、消耗积分生成视频，到查看、播放、下载、复制、删除历史视频的完整闭环；管理员端要覆盖模型配置、购买积分套餐、兑换码、用户状态和视频记录管理。

文档体系参考 Anthropic/Claude 对项目记忆的做法：根文档保持精炼，稳定背景放在全局文档里，细节按目录拆分；上下文文档只保存继续工作必需的信息。

## 目标

- 先完成可本地运行、可点击验证的 MVP。
- 后端保护模型 URL 和 Key，前端只请求自有 API。
- 积分体系支持购买套餐配置和后台生成兑换码。
- 视频生成记录、文件和管理员可见记录保留 3 天。
- 文档能支持中途中断后快速恢复，不依赖单个聊天窗口。

## 当前范围

- 用户端：登录注册、视频生成页、项目页、生成记录页、购买积分页、兑换积分页。
- 管理端：模型配置、套餐配置、兑换码管理、用户管理、视频记录、操作审计、系统设置。
- 后端：认证、积分、兑换码、管理员配置、视频任务接口、本地 Mock 视频任务状态机和真实供应商代码路径。
- 部署：优先支持单台美国服务器部署，当前已使用 PostgreSQL/Redis；后续可拆数据库和对象存储。

## 当前状态

- ~~用户端和管理端静态原型已完成主要交互。~~
- ~~后端 Fastify 服务已搭建，认证、兑换码、积分和视频任务基础接口已有测试。~~
- ~~本地服务已能启动：静态站点 `8765`，API `4000`。~~
- ~~Docker Desktop 已安装，本地 PostgreSQL 和 Redis 容器已启动。~~
- ~~PostgreSQL/Prisma 持久化已启用，后端重启后新注册用户、积分、兑换码和配置会保留。~~
- ~~兑换码已支持管理员批量生成 18 位大小写字母+数字，并支持永久有效。~~
- ~~用户端权限、隐私和关键空状态已收口：普通用户不显示管理员入口，兑换码输入不默认显示邮箱，空任务队列显示“暂无视频队列”，左下角显示“可用积分”并同步进度条。~~
- ~~第一批数据驱动闭环已完成：用户端模型、套餐、视频任务队列和项目页从后端加载；管理端模型、套餐、用户和视频记录列表及基础操作已接真实 API。~~
- ~~本地 Mock 视频任务闭环已完成：任务可流转到成功/失败，成功写入输出资产，失败退还积分且幂等。~~
- ~~管理后台交互已收口：模型、积分套餐和兑换码操作台默认为空；保存即新增或更新，保存后清空操作台；套餐和模型删除、启用/禁用都有明确失败提示。~~
- ~~用户管理已过滤管理员账号；模型配置删除支持历史生成记录引用场景，已引用模型会软删除并从配置和前台模型列表隐藏。~~
- ~~前端 API 请求头已收口：只有带 body 的请求才自动设置 `Content-Type: application/json`，避免空 body 的 DELETE 请求被后端 JSON 解析器拒绝。~~
- ~~真实视频供应商只读接口已跑通：后端已配置 OpenAI Video 兼容适配器，供应商 `GET /v1/models` 返回 `video-ds-2.0`、`video-ds-2.0-fast`。~~
- ~~模型配置加密 Key 已改为稳定环境变量读取，后端启动不再随机生成模型配置加密密钥。~~
- ~~管理后台模型名称已从供应商只读模型列表加载，保存为真实供应商模型 ID；模型别名可由管理员编辑，用户前台只展示别名但生成请求仍提交模型 ID。~~
- ~~真实视频任务提交与状态同步代码路径已接入：启用 `VIDEO_PROVIDER_REAL_JOBS=true` 后，用户点击生成会提交供应商 `POST /v1/videos`，状态同步调用 `GET /v1/videos/{id}`；常规测试仍使用 mocked fetch。~~
- ~~真实视频内容下载、本地文件存储、签名下载地址和 3 天过期清理已接入；默认存储目录可通过后端环境变量 `VIDEO_STORAGE_DIR` 覆盖。~~
- ~~用户兑换记录后端列表和用户端脱敏展示已完成：兑换积分页展示兑换时间、到账积分、批次和兑换码前后缀，不暴露完整兑换码、哈希或流水内部字段。~~
- ~~管理端操作审计已接入：新增 `/api/admin/audit-logs` 和后台“操作审计”页签，覆盖模型、套餐、兑换码批量生成、用户积分和封禁/解封操作，metadata 保持脱敏。~~
- ~~更完整错误提示已接入：后端返回结构化 `code/message/statusCode`，前端三页统一映射登录过期、封禁、余额不足、兑换码、模型/套餐/用户和供应商相关错误。~~
- ~~生产级模型 Key 轮换策略和旧密文迁移方案已接入：支持版本化 keyring、按记录 `keyVersion` 解密、旧数据缺失版本按 1 兼容，并提供 dry-run/`--apply` 迁移脚本。~~
- ~~真实视频状态同步已迁移到 Redis/BullMQ 后台同步器；前端轮询只刷新任务列表，不再触发供应商状态查询。~~
- ~~供应商错误详情解析已接入：真实供应商非 2xx 响应会解析 JSON/text 错误体，提取 `code/message/error_code/error_msg` 等字段，并避免输出 Key 明文。~~
- ~~已在明确批准下执行一次真实 `POST /v1/videos` 小流量联调：供应商返回 `HTTP 403: insufficient_user_quota - 用户额度不足, 剩余额度: ＄35.000000, 最低保留额度: ＄9.000000`；未产生 provider task id 或视频资产。~~
- ~~视频任务队列和项目视频已解耦：生成任务记录不可删除，项目页删除视频只删除视频资产记录，不影响任务记录。~~
- ~~项目页播放体验已接入：项目卡片“播放”复用视频资产签名地址，在项目卡片内联播放器直接播放；下载直接触发签名地址文件下载。~~
- ~~生产真实生成已放开：供应商额度已充值后，本地 `backend/.env` 已设置 `VIDEO_PROVIDER_REAL_JOBS=true`，后续用户点击生成视频会持续真实发起供应商请求，不再要求每次单独批准。~~
- ~~用户端视频生成页体验已继续收口：点击生成后立即提示“正在提交生成任务，请稍候...”，右侧预览区无任务时显示空状态，有活跃任务时显示“视频生成中...”，轮询检测完成后自动载入播放器并居中提示“视频已生成”；任务队列最多展示 10 条，在固定高度内滚动，底边与“生成参数”卡片底边对齐。~~
- ~~用户端新增“生成记录”页：展示生成时间、模型名称、提示词、尺寸/分辨率、用户选择的视频时长、参考图片/视频/音频数量、扣除积分、状态和视频生成时长；不再展示完成时间，不保存或展示视频地址/下载链接。~~
- ~~参考图/参考视频/参考音频已进入真实供应商提交 payload：图片最多 4 张传 `images`，视频最多 3 个传 `videos`，音频最多 1 个传 `audios`，后端仍只在真实供应商路径中使用后端 Key。~~
- ~~参考图片提交前会压缩成长边 1280px 的 JPEG data URL；后端拒绝超大的参考图片 data URL；失败任务队列卡片会显示失败原因摘要。~~
- ~~视频生成页真实生成体验继续修复：生成成功后停止重复轮询，避免已成功视频被持续刷新；任务队列只在排队/生成中状态保持轮询。~~
- ~~文生视频和视频生视频的参考素材控件已收口：文生视频参考图/首帧与参考音频同一行，视频生视频参考视频与参考音频同一行；提示词下方快捷风格按钮已移除。~~
- ~~项目页默认占位视频封面已移除：无视频资产时显示无视频空状态；项目卡片播放不再被写死封面顶住真实视频。~~
- ~~管理后台兑换码管理已升级：批量生成在上方，全部兑换码记录在下方，固定高度分页切换；管理员可查看完整兑换码、批次、积分、状态、生成时间、有效期和兑换信息；生成成功后弹出本次兑换码弹窗，弹窗内复制全部，复制格式为一行一个。完整码会通过现有 `code_ciphertext` 字段持久化，重载后可继续查看。~~
- ~~生成记录字段已补齐：用户端和管理员端均可查看提示词、模型展示名/供应商模型 ID、尺寸/画幅、分辨率、用户选择的视频时长、参考图片/视频/音频数量；管理员端和用户端均显示视频生成时长，按完成时间减提交时间计算，单位秒。~~
- ~~兑换码有效期已补齐：用户兑换记录、管理员兑换码记录和用户端左下角可用积分区域都展示兑换后的有效期/倒计时。~~
- ~~管理员后台兑换码管理已改为上下布局：批量生成在上方，全部兑换码记录在下方，固定高度分页切换。~~
- ~~项目页播放按钮不再弹出提示；生成参数下拉框已统一自定义样式；旧说明文案“文生视频和图生视频合并为一个入口；视频生视频独立上传参考视频。”已删除。~~
- ~~修复刷新后历史视频不渲染且按钮无法点击的问题：`index.html` 启动时先初始化 `redemptionRecords` 等状态变量，再回填本地用户信息，`updateCreditValidity()` 不再引用尚未初始化的状态默认值。~~
- ~~完成最新运营收口：项目页切换后会主动加载视频首帧作为封面；管理员后台系统提示改为居中弹窗；切换模型配置、积分套餐、用户管理、生成记录、操作审计时不再弹提示。~~
- ~~完成兑换码管理再收口：有效期改为“永久有效/自定义天数”，管理员复制历史兑换码时复制明文完整码，不再复制密文字段。~~
- ~~完成参考素材真实提交修复：前端仍以 data URL 提交图片/视频/音频，后端会保存参考素材到本地 `references/<jobId>/`，再把公网 HTTP(S) URL 提交给供应商的 `images`、`videos`、`audios` 字段，避免供应商拿到 base64 data URL 后返回 `task_id is empty`。~~
- ~~完成管理员“系统设置”：后台可保存公网 API 地址，生成任务转换参考素材 URL 时优先使用该数据库配置；未配置时回退 `PUBLIC_API_BASE_URL` 环境变量，再尝试从非本地域名请求头推断。新增 Prisma `system_settings` 表，部署需执行 `npm run prisma:deploy`。~~
- ~~完成供应商 TLS 证书错误友好提示：当供应商抓取参考素材 URL 时遇到 `x509` 证书过期/不可验证，后端映射为 `PUBLIC_API_BASE_URL_CERT_INVALID` 并提示更新公网 API 地址域名证书。~~
- 用户反馈真实生成已能成功生成视频；当前重点是继续观察真实闭环稳定性、参考素材公网访问稳定性和失败兜底。
- 最近验证：`npm test` 通过 18 个测试文件、136 个测试；`npm run build` 通过；`npm run prisma:generate` 和本地 `npm run prisma:deploy` 已用于新增 `system_settings`。
- 下一阶段重点是观察真实生成闭环稳定性：供应商 task id、后台同步、内容下载、本地资产、项目页播放/下载；同时继续补审计筛选/分页/导出和部署演练。

## 文档地图

- [文档总索引](docs/README.md)
- [产品文档](docs/product/README.md)
- [需求文档](docs/requirements/README.md)
- [架构文档](docs/architecture/README.md)
- [模块文档](docs/modules/README.md)
- [运维与部署](docs/operations/README.md)
- [上下文文档](docs/context/README.md)
- [新对话轻量接力提示词](docs/context/new-chat-prompt.md)
- [文档维护规则](docs/documentation-system.md)

## 关键入口

- 用户端页面：[index.html](index.html)
- 登录注册页：[auth.html](auth.html)
- 管理后台：[admin.html](admin.html)
- 后端说明：[backend/README.md](backend/README.md)
- 后端源码：[backend/src](backend/src)
- 数据库模型：[backend/prisma/schema.prisma](backend/prisma/schema.prisma)

## 本地运行

当前本地依赖：

```powershell
cd backend
docker compose up -d postgres redis
npm run prisma:deploy
```

用户端静态服务当前使用：

```powershell
python -m http.server 8765
```

后端服务当前使用：

```powershell
cd backend
npm run dev
```

默认 API 地址：

```text
http://127.0.0.1:4000
```

本地测试管理员：

```text
admin-code-1782584735007@example.com / password123
```

## 参考来源

- [Anthropic: Claude Code best practices](https://www.anthropic.com/engineering/claude-code-best-practices)
- [Anthropic Docs: Claude Code memory](https://docs.anthropic.com/en/docs/claude-code/memory)

## 部署更新

- GitHub 仓库部署入口：[docs/operations/debian-12-github-deployment-guide.md](docs/operations/debian-12-github-deployment-guide.md)。
- 生产服务器推荐 Debian 12.0 64bit，后端由 systemd 运行，Nginx 托管静态页面并反代 `/api/` 与 `/health`，PostgreSQL 保存业务数据，Redis 支撑真实视频任务状态同步。
- 同一台服务器可以再部署本项目，但需要独立目录、独立数据库、独立 systemd 服务名和不冲突的后端端口；若 `4000` 被占用，可改为 `4100` 并同步修改 Nginx `proxy_pass`。
- 新服务器不会自动继承本地数据库里的积分套餐、模型配置、系统设置或用户积分；除非做数据库备份恢复，否则上线后需要在管理后台重新配置。
- 上传参考素材生成真实任务要求公网 HTTPS 地址可访问 `/api/video/reference-assets/...`；本地未上线时，供应商访问参考素材 URL 会失败，即使 `/health` 返回正常。
